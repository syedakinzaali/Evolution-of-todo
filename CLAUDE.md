### How to Run
- Use `uv run main.py` to start the application.

### Build Commands
- Initialize project: `uv init`
- Install dependencies: `uv sync`

### Project Rules
- This is a Console-based Todo application for Phase 1.
- Logic is stored in `main.py`.
- Developed using AI-Native Spec-Driven development.